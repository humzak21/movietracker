# Example .env file for /home/squareds/.env
# Copy this to /home/squareds/.env and fill in your actual values

# Production Backend Configuration
NODE_ENV=production
PORT=3001

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# TMDB API Configuration
TMDB_API_KEY=your_tmdb_api_key
TMDB_BASE_URL=https://api.themoviedb.org/3

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration - CRITICAL: Update with your actual domain
# This MUST match your website's URL exactly (including https://)
# Example: FRONTEND_URL=https://yourdomain.com (no trailing slash)
FRONTEND_URL=https://yourdomain.com

# Optional: Database settings if needed
# DATABASE_URL=your_database_url 